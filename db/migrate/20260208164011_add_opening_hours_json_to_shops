# frozen_string_literal: true

class AddOpeningHoursJsonToShops < ActiveRecord::Migration[8.1]
def change
add_column :shops, :opening_hours_json, :jsonb, null: false, default: {}

# 使うなら（任意）
add_index :shops, :opening_hours_json, using: :gin
end
end

