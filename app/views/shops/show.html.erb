<%# /Users/kawamuratakuya/Desktop/吸えログデータ/dev/suelog/app/views/shops/show.html.erb %>

<% page_title_parts = [@shop.name.presence, "喫煙できる店", "吸えログ in大阪"].compact %>
<% page_description_parts = [] %>
<% page_description_parts << "#{@shop.name}の喫煙情報。" if @shop.name.present? %>
<% page_description_parts << @shop.smoking_area_label if @shop.respond_to?(:smoking_area_label) %>
<% page_description_parts << @shop.smoking_type_label if @shop.respond_to?(:smoking_type_label) %>
<% page_description_parts << "最寄駅：#{@shop.nearest_station}" if @shop.nearest_station.present? %>
<% page_description_parts << "住所：#{@shop.address}" if @shop.address.present? %>
<% if @shop.respond_to?(:display_opening_hours_text) && @shop.display_opening_hours_text.present? && @shop.display_opening_hours_text != "未設定" %>
<% page_description_parts << "営業時間：#{@shop.display_opening_hours_text}" %>
<% end %>
<% page_description_parts << "大阪で喫煙できる飲食店を探すなら吸えログ。" %>

<% content_for :title, page_title_parts.join("｜") %>
<% content_for :meta_description, page_description_parts.join(" ") %>
<% content_for :canonical_url, shop_url(@shop) %>

<% shop_location_text = [@shop.address, @shop.nearest_station, @shop.name].compact.join(" ") %>
<% is_umeda_shop = shop_location_text.include?("梅田") %>
<% is_namba_shop = shop_location_text.include?("難波") || shop_location_text.include?("なんば") %>

<% back_path =
if is_umeda_shop
umeda_path
elsif is_namba_shop
namba_path
else
root_path
end %>

<% back_label =
if is_umeda_shop
"← 梅田の一覧に戻る"
elsif is_namba_shop
"← 難波の一覧に戻る"
else
"← 一覧に戻る"
end %>

<% footer_back_label =
if is_umeda_shop
"梅田の一覧に戻る"
elsif is_namba_shop
"難波の一覧に戻る"
else
"一覧に戻る"
end %>

<% content_for :head do %>
<% photo_url = nil %>
<% if @shop.respond_to?(:food_photos) && @shop.food_photos.attached? %>
<% photo_url = url_for(@shop.food_photos.first) rescue nil %>
<% elsif @shop.respond_to?(:exterior_photos) && @shop.exterior_photos.attached? %>
<% photo_url = url_for(@shop.exterior_photos.first) rescue nil %>
<% elsif @shop.respond_to?(:interior_photos) && @shop.interior_photos.attached? %>
<% photo_url = url_for(@shop.interior_photos.first) rescue nil %>
<% elsif @shop.respond_to?(:menu_photos) && @shop.menu_photos.attached? %>
<% photo_url = url_for(@shop.menu_photos.first) rescue nil %>
<% end %>

<% restaurant_structured_data = {
"@context": "https://schema.org",
"@type": "Restaurant",
"name": @shop.name,
"url": shop_url(@shop),
"image": photo_url,
"telephone": @shop.phone.presence,
"address": (@shop.address.present? ? {
"@type": "PostalAddress",
"streetAddress": @shop.address,
"addressRegion": "大阪府",
"addressCountry": "JP"
} : nil),
"servesCuisine": @shop.display_genre.presence,
"description": page_description_parts.join(" "),
"smokingAllowed": (@shop.smoking_area == "all_smoking"),
"keywords": [
(@shop.smoking_area_label if @shop.respond_to?(:smoking_area_label)),
(@shop.smoking_type_label if @shop.respond_to?(:smoking_type_label)),
@shop.display_genre.presence,
@shop.nearest_station.presence,
"大阪",
"喫煙可"
].compact.join(", "),
"geo": (@shop.latitude.present? && @shop.longitude.present? ? {
"@type": "GeoCoordinates",
"latitude": @shop.latitude,
"longitude": @shop.longitude
} : nil)
}.compact %>

<% breadcrumb_items = [
{
"@type": "ListItem",
"position": 1,
"name": "ホーム",
"item": root_url
}
] %>

<% if is_umeda_shop %>
<% breadcrumb_items << {
"@type": "ListItem",
"position": 2,
"name": "梅田で喫煙できる店",
"item": umeda_url
} %>
<% breadcrumb_items << {
"@type": "ListItem",
"position": 3,
"name": @shop.name
} %>
<% elsif is_namba_shop %>
<% breadcrumb_items << {
"@type": "ListItem",
"position": 2,
"name": "難波で喫煙できる店",
"item": namba_url
} %>
<% breadcrumb_items << {
"@type": "ListItem",
"position": 3,
"name": @shop.name
} %>
<% else %>
<% breadcrumb_items << {
"@type": "ListItem",
"position": 2,
"name": @shop.name
} %>
<% end %>

<% breadcrumb_structured_data = {
"@context": "https://schema.org",
"@type": "BreadcrumbList",
"itemListElement": breadcrumb_items
} %>

<script type="application/ld+json">
<%= raw restaurant_structured_data.to_json %>
</script>

<script type="application/ld+json">
<%= raw breadcrumb_structured_data.to_json %>
</script>
<% end %>

<style>
.shop-show {
max-width: 980px;
margin: 0 auto;
}

.shop-show__breadcrumb {
margin: 0 0 14px;
font-size: 13px;
color: #666666;
line-height: 1.7;
}

.shop-show__breadcrumb a {
color: #666666;
text-decoration: none;
}

.shop-show__breadcrumbCurrent {
color: #111111;
font-weight: 700;
}

.shop-show__back {
margin: 0 0 14px;
}

.shop-show__backLink {
display: inline-flex;
align-items: center;
color: #6b6b6b;
text-decoration: none;
font-weight: 700;
}

.shop-show__backLink:hover {
color: #111111;
}

.shop-show__hero {
background: linear-gradient(135deg, #fffdf8 0%, #ffffff 60%);
border: 1px solid #e8e3d7;
border-radius: 22px;
padding: 22px;
box-shadow: 0 10px 30px rgba(17, 17, 17, 0.04);
margin-bottom: 18px;
}

.shop-show__title {
margin: 0 0 8px;
font-size: clamp(28px, 4vw, 40px);
line-height: 1.15;
letter-spacing: -0.03em;
color: #111111;
}

.shop-show__sub {
margin: 0;
color: #6b6b6b;
font-size: 14px;
}

.shop-show__badges {
display: flex;
gap: 8px;
flex-wrap: wrap;
margin-top: 14px;
}

.shop-show__lead {
margin-top: 16px;
color: #333333;
font-size: 15px;
line-height: 1.9;
}

.shop-show__layout {
display: grid;
grid-template-columns: minmax(0, 1fr);
gap: 16px;
}

.shop-show__section {
background: #ffffff;
border: 1px solid #e8e3d7;
border-radius: 20px;
padding: 18px;
box-shadow: 0 10px 30px rgba(17, 17, 17, 0.03);
}

.shop-show__sectionTitle {
margin: 0 0 14px;
font-size: 20px;
font-weight: 800;
color: #111111;
letter-spacing: -0.02em;
}

.shop-show__metaGrid {
display: grid;
grid-template-columns: repeat(2, minmax(0, 1fr));
gap: 12px;
}

.shop-show__metaItem {
background: #faf8f2;
border: 1px solid #eee6d6;
border-radius: 16px;
padding: 14px;
min-width: 0;
}

.shop-show__metaLabel {
display: block;
margin-bottom: 6px;
color: #7a7a7a;
font-size: 12px;
font-weight: 700;
}

.shop-show__metaValue {
color: #111111;
font-size: 15px;
line-height: 1.7;
word-break: break-word;
white-space: pre-wrap;
}

.shop-show__hoursBox {
display: grid;
gap: 12px;
}

.shop-show__hoursCard {
background: #faf8f2;
border: 1px solid #eee6d6;
border-radius: 16px;
padding: 14px;
}

.shop-show__hoursCardTitle {
margin: 0 0 6px;
color: #7a7a7a;
font-size: 12px;
font-weight: 700;
}

.shop-show__hoursCardValue {
color: #111111;
font-size: 15px;
line-height: 1.8;
white-space: pre-wrap;
word-break: break-word;
}

.shop-show__hoursNote {
margin-top: 8px;
color: #777777;
font-size: 12px;
}

.shop-show__phoneLink {
display: inline-flex;
align-items: center;
padding: 10px 14px;
background: #f6edd7;
color: #7a5a12;
border: 1px solid #eadbb5;
border-radius: 10px;
text-decoration: none;
font-weight: 800;
}

.shop-show__bookingActions {
display: flex;
gap: 10px;
flex-wrap: wrap;
margin-top: 8px;
}

.shop-show__map {
height: 320px;
min-height: 320px;
border: 1px solid #e0e0e0;
border-radius: 14px;
overflow: hidden;
background: #f7f7f7;
}

.shop-show__mapLink {
margin-top: 10px;
}

.shop-show__mapLink a {
color: #7a5a12;
font-weight: 700;
text-decoration: none;
}

.shop-show__photosGroup + .shop-show__photosGroup {
margin-top: 18px;
}

.shop-show__photosTitle {
margin: 0 0 10px;
font-size: 16px;
font-weight: 800;
color: #111111;
}

.shop-show__photosGrid {
display: flex;
gap: 10px;
flex-wrap: wrap;
}

.shop-show__photo {
width: 160px;
height: 160px;
object-fit: cover;
border-radius: 12px;
border: 1px solid #ddd;
}

.shop-show__footerActions {
display: flex;
gap: 10px;
flex-wrap: wrap;
margin-top: 4px;
}

.shop-show__relatedLinks {
display: flex;
gap: 10px;
flex-wrap: wrap;
margin-top: 8px;
}

.shop-show__divider {
border: 0;
border-top: 1px solid #eee6d6;
margin: 4px 0 0;
}

.ss-badge {
display: inline-flex;
align-items: center;
min-height: 32px;
padding: 0 10px;
border-radius: 999px;
font-size: 12px;
font-weight: 800;
border: 1px solid transparent;
}

.ss-badge--dark {
background: #111111;
color: #ffffff;
border-color: #111111;
}

.ss-badge--gold {
background: #f6edd7;
color: #7a5a12;
border-color: #eadbb5;
}

.ss-badge--line {
background: #ffffff;
color: #111111;
border-color: #e8e3d7;
}

.ss-badge--green {
background: #eef7ee;
color: #2f6b3b;
border-color: #d8e7d7;
}

.ss-badge--warn {
background: #fff6db;
color: #8a6500;
border-color: #f0de9c;
}

.ss-badge--muted {
background: #f5f5f5;
color: #555555;
border-color: #d8d8d8;
}

.ss-btn {
display: inline-flex;
align-items: center;
justify-content: center;
min-height: 42px;
padding: 0 16px;
border-radius: 999px;
text-decoration: none;
font-size: 14px;
font-weight: 800;
border: 1px solid transparent;
}

.ss-btn--dark {
background: #111111;
color: #ffffff;
border-color: #111111;
}

.ss-btn--ghost {
background: #ffffff;
color: #111111;
border-color: #e8e3d7;
}

.ss-btn--muted {
background: #ffffff;
color: #666666;
border-color: #eeeeee;
}

.ss-alert {
padding: 12px 14px;
border-radius: 12px;
margin: 10px 0;
font-weight: 700;
}

.ss-alert--notice {
border: 1px solid #d8e7d7;
background: #eef7ee;
color: #2f6b3b;
}

.ss-alert--alert {
border: 1px solid #f0d1d1;
background: #fff6f6;
color: #b00020;
}

.ss-alert--warn {
border: 1px solid #f0de9c;
background: #fffaf0;
color: #8a6500;
}

.ss-pending {
border: 1px solid #f0d1d1;
background: #fff6f6;
padding: 14px;
border-radius: 14px;
}

.ss-pending__title {
margin: 0;
color: #b00020;
font-weight: 800;
}

.ss-pending__text {
margin: 6px 0 0;
color: #666666;
}

@media (max-width: 720px) {
.shop-show__hero,
.shop-show__section {
border-radius: 16px;
padding: 16px;
}

.shop-show__metaGrid {
grid-template-columns: 1fr;
}

.shop-show__footerActions,
.shop-show__relatedLinks,
.shop-show__bookingActions {
flex-direction: column;
}

.ss-btn {
width: 100%;
}

.shop-show__photo {
width: calc(50% - 5px);
height: 140px;
}
}
</style>

<div class="shop-show">
<nav class="shop-show__breadcrumb" aria-label="パンくず">
<%= link_to "ホーム", root_path %>

<% if is_umeda_shop %>
<span> › </span>
<%= link_to "梅田で喫煙できる店", umeda_path %>
<% elsif is_namba_shop %>
<span> › </span>
<%= link_to "難波で喫煙できる店", namba_path %>
<% end %>

<span> › </span>
<span class="shop-show__breadcrumbCurrent"><%= @shop.name %></span>
</nav>

<div class="shop-show__back">
<%= link_to back_label, back_path, class: "shop-show__backLink" %>
</div>

<section class="shop-show__hero">
<h1 class="shop-show__title"><%= @shop.name %></h1>
<p class="shop-show__sub">
ジャンル：<%= @shop.display_genre.presence || "未設定" %>
</p>

<div class="shop-show__badges">
<% if @shop.smoking_area == "all_smoking" %>
<span class="ss-badge ss-badge--dark">席で喫煙可</span>
<% elsif @shop.smoking_area == "separated" %>
<span class="ss-badge ss-badge--line">喫煙所あり</span>
<% elsif @shop.smoking_area == "unknown" %>
<span class="ss-badge ss-badge--muted">喫煙エリア不明</span>
<% end %>

<% if @shop.smoking_type == "both_ok" %>
<span class="ss-badge ss-badge--gold">紙・加熱式OK</span>
<% elsif @shop.smoking_type == "electronic_only" %>
<span class="ss-badge ss-badge--gold">加熱式のみ</span>
<% elsif @shop.smoking_type == "paper_only" %>
<span class="ss-badge ss-badge--gold">紙のみ</span>
<% elsif @shop.smoking_type == "unknown" %>
<span class="ss-badge ss-badge--muted">喫煙タイプ不明</span>
<% end %>

<% if @shop.respond_to?(:smoking_unverified?) && @shop.smoking_unverified? %>
<span class="ss-badge ss-badge--warn">喫煙情報未確認</span>
<% end %>

<% if @shop.respond_to?(:open_now?) && @shop.open_now? %>
<span class="ss-badge ss-badge--green">営業中</span>
<% end %>

<% if @shop.last_confirmed_on.present? %>
<span class="ss-badge ss-badge--line">最終確認：<%= @shop.last_confirmed_on %></span>
<% end %>
</div>

<p class="shop-show__lead">
<%= @shop.name %>は
<%= @shop.respond_to?(:smoking_area_label) ? @shop.smoking_area_label : "喫煙情報未設定" %>、
<%= @shop.respond_to?(:smoking_type_label) ? @shop.smoking_type_label : "喫煙タイプ未設定" %>の店舗です。
<% if @shop.nearest_station.present? %>
最寄駅は<%= @shop.nearest_station %>。
<% end %>
<% if @shop.respond_to?(:display_opening_hours_text) && @shop.display_opening_hours_text.present? && @shop.display_opening_hours_text != "未設定" %>
通常営業時間は「<%= @shop.display_opening_hours_text %>」です。
<% end %>
大阪で喫煙できる飲食店を探している方の参考用ページです。
</p>
</section>

<% if flash[:notice].present? %>
<div class="ss-alert ss-alert--notice"><%= flash[:notice] %></div>
<% end %>

<% if flash[:alert].present? %>
<div class="ss-alert ss-alert--alert"><%= flash[:alert] %></div>
<% end %>

<% if @shop.respond_to?(:smoking_unverified?) && @shop.smoking_unverified? %>
<div class="ss-alert ss-alert--warn">
この店舗の喫煙情報は未確認です。来店前に店舗へご確認ください。
</div>
<% end %>

<div class="shop-show__layout">
<section class="shop-show__section">
<h2 class="shop-show__sectionTitle">基本情報</h2>

<div class="shop-show__metaGrid">
<div class="shop-show__metaItem">
<span class="shop-show__metaLabel">住所</span>
<div class="shop-show__metaValue"><%= @shop.address.presence || "未設定" %></div>
</div>

<div class="shop-show__metaItem">
<span class="shop-show__metaLabel">最寄駅</span>
<div class="shop-show__metaValue"><%= @shop.nearest_station.presence || "未設定" %></div>
</div>

<div class="shop-show__metaItem">
<span class="shop-show__metaLabel">電話番号</span>
<div class="shop-show__metaValue">
<% if @shop.phone.present? %>
<a href="tel:<%= @shop.phone.gsub(/[^0-9]/, "") %>" class="shop-show__phoneLink">
<%= @shop.phone %>
</a>
<% else %>
未設定
<% end %>
</div>
</div>

<div class="shop-show__metaItem">
<span class="shop-show__metaLabel">喫煙エリア</span>
<div class="shop-show__metaValue">
<%= @shop.respond_to?(:smoking_area_label) ? @shop.smoking_area_label : "未設定" %>
</div>
</div>

<div class="shop-show__metaItem">
<span class="shop-show__metaLabel">喫煙タイプ</span>
<div class="shop-show__metaValue">
<%= @shop.respond_to?(:smoking_type_label) ? @shop.smoking_type_label : "未設定" %>
</div>
</div>

<% if @shop.respond_to?(:smoking_unverified?) && @shop.smoking_unverified? %>
<div class="shop-show__metaItem">
<span class="shop-show__metaLabel">喫煙情報ステータス</span>
<div class="shop-show__metaValue">未確認</div>
</div>
<% end %>
</div>
</section>

<section class="shop-show__section">
<h2 class="shop-show__sectionTitle">営業時間</h2>

<div class="shop-show__hoursBox">
<div class="shop-show__hoursCard">
<p class="shop-show__hoursCardTitle">通常営業時間</p>
<div class="shop-show__hoursCardValue">
<%= @shop.respond_to?(:display_opening_hours_text) ? @shop.display_opening_hours_text : "未設定" %>
</div>
</div>

<div class="shop-show__hoursCard">
<p class="shop-show__hoursCardTitle">祝日営業時間</p>
<div class="shop-show__hoursCardValue">
<%= @shop.respond_to?(:display_holiday_hours_text) ? @shop.display_holiday_hours_text : "未設定" %>
</div>
</div>

<div class="shop-show__hoursCard">
<p class="shop-show__hoursCardTitle">定休日</p>
<div class="shop-show__hoursCardValue">
<%= @shop.respond_to?(:display_closed_days_text) ? @shop.display_closed_days_text : "未設定" %>
</div>
</div>
</div>

<p class="shop-show__hoursNote">
※ 営業時間は店舗情報・ユーザー投稿を元に表示しています
</p>
</section>

<% unless @shop.approved? %>
<section class="shop-show__section">
<div class="ss-pending">
<p class="ss-pending__title">この店舗は承認待ちです</p>
<p class="ss-pending__text">※ 詳細（地図・写真・口コミ・メモ等）は確認後に公開されます</p>
</div>
</section>
<% else %>
<section class="shop-show__section">
<h2 class="shop-show__sectionTitle">予約・詳細</h2>

<div class="shop-show__bookingActions">
<% if @shop.phone.present? %>
<a href="tel:<%= @shop.phone.gsub(/[^0-9]/, "") %>" class="ss-btn ss-btn--dark">
電話予約する
</a>
<% end %>

<% if @shop.respond_to?(:tabelog_url) && @shop.tabelog_url.present? %>
<%= link_to "食べログで見る",
@shop.tabelog_url,
target: "_blank",
rel: "nofollow noopener",
class: "ss-btn ss-btn--ghost" %>
<% end %>

<% if @shop.respond_to?(:hotpepper_url) && @shop.hotpepper_url.present? %>
<%= link_to "ホットペッパーで予約",
@shop.hotpepper_url,
target: "_blank",
rel: "nofollow noopener",
class: "ss-btn ss-btn--dark" %>
<% end %>
</div>
</section>

<section class="shop-show__section">
<h2 class="shop-show__sectionTitle">補足情報</h2>

<div class="shop-show__metaGrid">
<div class="shop-show__metaItem">
<span class="shop-show__metaLabel">メモ</span>
<div class="shop-show__metaValue">
<%= @shop.respond_to?(:note) ? (@shop.note.presence || "未設定") : "未設定" %>
</div>
</div>

<div class="shop-show__metaItem">
<span class="shop-show__metaLabel">最終確認日</span>
<div class="shop-show__metaValue"><%= @shop.last_confirmed_on.presence || "未設定" %></div>
</div>
</div>
</section>

<section class="shop-show__section">
<h2 class="shop-show__sectionTitle">地図</h2>

<% map_query = ERB::Util.url_encode([@shop.name, @shop.address].compact.join(" ")) %>
<% address_query = ERB::Util.url_encode(@shop.address.to_s) %>

<% if @shop.latitude.present? && @shop.longitude.present? %>
<div id="shop-map" class="shop-show__map"></div>

<p class="shop-show__mapLink">
<a href="https://www.google.com/maps?q=<%= map_query %>" target="_blank" rel="noopener">Googleマップで開く</a>
</p>

<script>
(function() {
function loadLeafletCss() {
if (document.getElementById("leaflet-css")) return;
const link = document.createElement("link");
link.id = "leaflet-css";
link.rel = "stylesheet";
link.href = "https://unpkg.com/leaflet@1.9.4/dist/leaflet.css";
document.head.appendChild(link);
}

function loadLeafletJs(callback) {
if (window.L) {
callback();
return;
}

if (document.getElementById("leaflet-js")) {
document.getElementById("leaflet-js").addEventListener("load", callback, { once: true });
return;
}

const script = document.createElement("script");
script.id = "leaflet-js";
script.src = "https://unpkg.com/leaflet@1.9.4/dist/leaflet.js";
script.async = true;
script.onload = callback;
document.body.appendChild(script);
}

function initShopMap() {
const el = document.getElementById("shop-map");
if (!el || !window.L) return;

if (el._leaflet_map_instance) {
el._leaflet_map_instance.remove();
el._leaflet_map_instance = null;
}

const lat = <%= @shop.latitude.to_f %>;
const lng = <%= @shop.longitude.to_f %>;

const map = L.map(el).setView([lat, lng], 16);
el._leaflet_map_instance = map;

L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
maxZoom: 19,
attribution: '&copy; OpenStreetMap contributors'
}).addTo(map);

const marker = L.marker([lat, lng]).addTo(map);
marker.bindPopup("<strong><%= j @shop.name %></strong>").openPopup();

setTimeout(() => map.invalidateSize(), 50);
setTimeout(() => map.invalidateSize(), 300);
}

function bootShopMap() {
loadLeafletCss();
loadLeafletJs(initShopMap);
}

document.addEventListener("turbo:load", bootShopMap);
document.addEventListener("DOMContentLoaded", bootShopMap);
document.addEventListener("turbo:before-cache", () => {
const el = document.getElementById("shop-map");
if (el && el._leaflet_map_instance) {
el._leaflet_map_instance.remove();
el._leaflet_map_instance = null;
}
});

bootShopMap();
})();
</script>
<% elsif @shop.address.present? %>
<div class="shop-show__map">
<iframe
src="https://maps.google.com/maps?q=<%= address_query %>&z=16&output=embed"
width="100%"
height="320"
style="border:0; display:block;"
loading="lazy"
referrerpolicy="no-referrer-when-downgrade"
allowfullscreen>
</iframe>
</div>

<p class="shop-show__mapLink">
<a href="https://www.google.com/maps/search/?api=1&query=<%= address_query %>" target="_blank" rel="noopener">Googleマップで開く</a>
</p>

<p class="shop-show__hoursNote">
※ 緯度・経度が未設定のため、住所から地図を表示しています
</p>
<% else %>
<p style="color:#666;">この店舗は位置情報（緯度・経度）も住所も未設定です。</p>
<% end %>
</section>

<section class="shop-show__section">
<h2 class="shop-show__sectionTitle">写真</h2>

<% [["料理", @shop.food_photos], ["内観", @shop.interior_photos], ["外観", @shop.exterior_photos], ["メニュー", @shop.menu_photos]].each do |label, photos| %>
<div class="shop-show__photosGroup">
<h3 class="shop-show__photosTitle"><%= label %></h3>

<% if photos.attached? %>
<div class="shop-show__photosGrid">
<% photos.each do |p| %>
<%= image_tag url_for(p), class: "shop-show__photo" %>
<% end %>
</div>
<% else %>
<p style="color:#666;">まだありません</p>
<% end %>
</div>
<% end %>
</section>
<% end %>

<% if is_umeda_shop || is_namba_shop %>
<section class="shop-show__section">
<h2 class="shop-show__sectionTitle">関連エリアページ</h2>
<p style="color:#666; margin:0 0 12px;">
このエリアの他の喫煙可能店も探せます。
</p>

<div class="shop-show__relatedLinks">
<% if is_umeda_shop %>
<%= link_to "梅田で喫煙できる店一覧", umeda_path, class: "ss-btn ss-btn--ghost" %>
<%= link_to "梅田で席で喫煙可", umeda_smoking_path("all_smoking"), class: "ss-btn ss-btn--ghost" %>
<%= link_to "梅田で喫煙所あり", umeda_smoking_path("separated"), class: "ss-btn ss-btn--ghost" %>
<%= link_to "梅田の居酒屋", umeda_genre_path("izakaya"), class: "ss-btn ss-btn--ghost" %>
<% end %>

<% if is_namba_shop %>
<%= link_to "難波で喫煙できる店一覧", namba_path, class: "ss-btn ss-btn--ghost" %>
<%= link_to "難波で席で喫煙可", namba_smoking_path("all_smoking"), class: "ss-btn ss-btn--ghost" %>
<%= link_to "難波で喫煙所あり", namba_smoking_path("separated"), class: "ss-btn ss-btn--ghost" %>
<%= link_to "難波の居酒屋", namba_genre_path("izakaya"), class: "ss-btn ss-btn--ghost" %>
<% end %>
</div>
</section>
<% end %>

<hr class="shop-show__divider">

<section class="shop-show__section">
<h2 class="shop-show__sectionTitle">このページについて</h2>
<div class="shop-show__footerActions">
<%= link_to footer_back_label, back_path, class: "ss-btn ss-btn--ghost" %>
<%= link_to "情報の修正依頼", new_shop_shop_edit_request_path(@shop), class: "ss-btn ss-btn--dark" %>
<%= link_to "通報", new_shop_shop_report_path(@shop), class: "ss-btn ss-btn--muted" %>
</div>
</section>
</div>
</div>